---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ArchiveView from '../../components/ArchiveView.astro';
import { useStrings } from '../../lib/i18n';

const strings = useStrings('en');
---

<BaseLayout locale="en" title={strings.archive.title} description={strings.archive.lede}>
  <ArchiveView locale="en" />
</BaseLayout>
